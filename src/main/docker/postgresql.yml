version: '2'
services:
    mybizdev-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/mybizdev/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=admin
        ports:
            - 5432:5432
